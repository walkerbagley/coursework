(define-module (ice-9 paradigms_d3))

; these are just implementaions to emulate a situation
; where we only have limited hardware

(define-public add1
  (lambda (n)
    (+ n 1)))

(define-public sub1
  (lambda (n)
    (- n 1)))

